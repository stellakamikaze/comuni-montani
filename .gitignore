# Sorgenti con dati in chiaro: NON pubblicare (vanificherebbe la password)
dati.json
_*.json
template.html
build.js
encrypted/
node_modules/
.playwright-mcp/
*.png

index.html
.staticrypt.json

# Handoff: contengono la password, restano solo nel mirror privato ~/.claude
HANDOFF_*.md
